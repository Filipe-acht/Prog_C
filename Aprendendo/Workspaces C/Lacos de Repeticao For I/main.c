#include <stdio.h>
#include <stdlib.h>

int main()
{
    //while(true)
    //do/while(true)
    /*
    for(parte1; parte2; parte3){
        parte1 -> é onde nós declaramos e iniciliazimos a nossa variavel
        parte2 -> é a condição
        parte3 -> é onde nós incrementamos a nossa variável

    }
    */

    for(int i = 0; i <= 10; ++i){//pré-incremento
        printf("%i\n", i);
    }


    return 0;
}
